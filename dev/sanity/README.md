# Sanity Studio

## Quick start

1. Install dependencies:

```bash
npm install
```

2. Update project configuration in `sanity.config.ts`:

- `projectId`
- `dataset`

3. Start studio:

```bash
npm run dev
```

## Included schemas

- `post`
- `author`
- `tag`
- `page`
- `siteSettings`
