# Web Test Implementation - Continuation Plan

## Where We Left Off

Phases 1-5 are complete (~144 new tests passing). The following remain:

### Phase 6: shadcn UI components (4 tests to write + 1 to verify)

1. **Verify `skeleton.test.tsx`** - Already written at `src/components/ui/__tests__/skeleton.test.tsx`, needs to be run to confirm passing
2. **Write `input.test.tsx`** - Test: renders `<input>` element, applies default classes, custom className, disabled state, ref forwarding, type prop
3. **Write `textarea.test.tsx`** - Test: renders `<textarea>` element, applies default classes, custom className, disabled state, ref forwarding
4. **Write `separator.test.tsx`** - Test: renders horizontal by default, renders vertical when set, applies className, decorative prop, uses Radix Separator primitive
5. **Write `tooltip.test.tsx`** - Test: TooltipProvider renders children, TooltipTrigger renders button, TooltipContent renders with classes, custom className on content, sideOffset default

### Phase 7: CI Integration

6. **Edit `.github/workflows/ci.yml`** - Add a `test:run` step between "Install deps" and "Build" in the `web-build` job:
   ```yaml
   - name: Test
     run: pnpm run test:run
   ```

### Final

7. **Run full test suite** - `pnpm run test:run` from `dev/web/` to verify all tests pass
8. **Run lint/typecheck** - Ensure no regressions from source code changes (exports added to `api.ts`, `loader.ts`, `CmsRichText.tsx`)

## Files to Create/Modify

```
CREATE dev/web/src/components/ui/__tests__/input.test.tsx
CREATE dev/web/src/components/ui/__tests__/textarea.test.tsx
CREATE dev/web/src/components/ui/__tests__/separator.test.tsx
CREATE dev/web/src/components/ui/__tests__/tooltip.test.tsx
EDIT   .github/workflows/ci.yml (add test:run step)
```

## Estimated Additional Tests: ~25
