[build]
base = "dev/web"
command = "pnpm install --frozen-lockfile && pnpm run build"
publish = "dist"

[build.environment]
NODE_VERSION = "20.19"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
